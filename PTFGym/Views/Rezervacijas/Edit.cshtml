@model PTFGym.Models.Rezervacija

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Rezervacija</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="ClanId" class="control-label">Clanovi</label>
                <select asp-for="ClanId" class="form-control" asp-items="ViewBag.Clanovi"></select>
                <span asp-validation-for="ClanId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="TrenerId" class="control-label"></label>
                <select asp-for="TrenerId" class="form-control" asp-items="ViewBag.Treneri">Treneri</select>
                <span asp-validation-for="TrenerId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="DatumRezervacije" class="control-label"></label>
                <input asp-for="DatumRezervacije" class="form-control" />
                <span asp-validation-for="DatumRezervacije" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
